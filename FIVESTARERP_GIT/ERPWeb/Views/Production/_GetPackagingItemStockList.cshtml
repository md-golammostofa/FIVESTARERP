@model IEnumerable<ERPBO.Production.ViewModels.PackagingItemStockInfoViewModel>
@{
    //Layout = null;
    //var pagerData = (ERPWeb.Infrastructure.PagerData)ViewBag.PagerData;
    int sl = 0;//pagerData.Serial;
    string badge = string.Empty;
    //var privilege = (ERPBO.Common.UserPrivilege)ViewBag.UserPrivilege;
    var qty = 0;
}
<div class="row">
    <div class="col-md-12 text-sm">
        <table class="table table-bordered table-striped table-sm table-responsive-lg">
            <thead class="btn-dark">
                <tr class="text-center text-sm text-bold">
                    <td style="vertical-align:middle">#SL</td>
                    <td style="vertical-align:middle">Floor</td>
                    <td style="vertical-align:middle">Packaging Line</td>
                    <td style="vertical-align:middle">Model</td>

                    <td style="vertical-align:middle">Warehouse</td>
                    <td style="vertical-align:middle">ItemType</td>
                    <td style="vertical-align:middle">Item-Color</td>
                    <td style="vertical-align:middle">In-Quantity</td>
                    <td style="vertical-align:middle">Transfer/Finish Qty.</td>
                    <td style="vertical-align:middle">Current Stock</td>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    sl = sl + 1;
                    qty = (item.Quantity - item.TransferQty);
                    <tr>
                        <td class="text-center text-bold">@sl</td>
                        <td>@item.ProductionFloorName</td>
                        <td>@item.PackagingLineName</td>
                        <td class="text-center">@item.ModelName</td>
                        <td>@item.WarehouseName</td>
                        <td>@item.ItemTypeName</td>
                        <td>@item.ItemName</td>
                        <td class="text-center text-bold">@item.Quantity</td>
                        <td class="text-center text-bold">@item.TransferQty</td>
                        <td class="text-center text-bold">@qty</td>
                    </tr>
                }
                @if (Model.Count() == 0)
                {
                    <tr class="text-center text-bold">
                        <td colspan="11">No data found in the table</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <div class="col-md-12">
        @*@Html.Partial("_Pagination", pagerData)*@
    </div>
</div>


